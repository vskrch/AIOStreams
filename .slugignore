# Ignore source files (TypeScript) - but keep .d.ts type definitions
# Note: .slugignore doesn't support negation, so we list specific patterns
*.test.ts
*.spec.ts

# Ignore documentation and config not needed at runtime
*.md
.git
.github
.vscode
branding
.prettierrc
tsconfig*.json
release-please-config.json
.release-please-manifest.json
compose.yaml

# Ignore source directories (compiled JS is in dist)
packages/core/src
packages/server/src
packages/frontend/src
packages/frontend/.next
